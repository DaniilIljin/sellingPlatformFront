export type UserRegisterDTO = {
    name: string;
    fullName: string;
    email: string;
    address: string;
    additionalInfo: string;
    password: string;
}

export type UserLoginDTO = {
    name: string;
    password: string;
}

